# In tests there is no catalog running: point gRPC at a closed port so the
# startup runner fails fast (UNAVAILABLE) instead of waiting on whatever might
# be listening on the default 9090.
rules.source=grpc
rules.catalog.grpc-target=localhost:1
