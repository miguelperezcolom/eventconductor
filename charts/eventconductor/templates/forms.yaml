## ── Forms Deployment ─────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "eventconductor.fullname" . }}-forms
  labels:
    {{- include "eventconductor.labels" . | nindent 4 }}
    app.kubernetes.io/component: forms
spec:
  replicas: {{ .Values.forms.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: forms
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: forms
    spec:
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z postgres 5432; do echo waiting for postgres; sleep 2; done']
        - name: wait-for-redpanda
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z redpanda 19092; do echo waiting for redpanda; sleep 2; done']
      containers:
        - name: forms
          image: {{ .Values.forms.image }}
          ports:
            - containerPort: {{ .Values.forms.port }}
          env:
            - name: SERVER_PORT
              value: {{ .Values.forms.port | quote }}
            - name: DB_URL
              value: jdbc:postgresql://postgres:5432/{{ .Values.postgres.database }}
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "eventconductor.fullname" . }}-postgres
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "eventconductor.fullname" . }}-postgres
                  key: POSTGRES_PASSWORD
            - name: KAFKA_BROKERS
              value: {{ .Values.redpanda.brokerInternal }}
            - name: DDL_AUTO
              value: {{ .Values.forms.ddlAuto }}
          resources:
            {{- toYaml .Values.forms.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.forms.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.forms.port }}
            initialDelaySeconds: 60
            periodSeconds: 20
---
## ── Forms Service ────────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: forms
  labels:
    {{- include "eventconductor.labels" . | nindent 4 }}
    app.kubernetes.io/component: forms
spec:
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: forms
  ports:
    - port: {{ .Values.forms.servicePort }}
      targetPort: {{ .Values.forms.port }}
