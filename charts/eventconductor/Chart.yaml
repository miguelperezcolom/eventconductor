apiVersion: v2
name: eventconductor
description: EventConductor workflow orchestration — Redpanda, PostgreSQL, orchestrator and forms.
type: application
version: 0.1.0
# Pinned to a real release in CI. The chart fails to template when this is
# the literal string "latest" so users notice if they forget to override it.
appVersion: "0.0.0"
maintainers:
  - name: Miguel Pérez Colom
    url: https://github.com/miguelperezcolom
sources:
  - https://github.com/miguelperezcolom/eventconductor
keywords:
  - workflow
  - orchestration
  - event-driven
  - spring-boot
