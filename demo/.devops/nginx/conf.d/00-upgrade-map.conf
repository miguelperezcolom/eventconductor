# Maps the Upgrade request header to the right Connection value so WebSocket /
# server-push connections are proxied correctly (used by ec-https.conf).
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
