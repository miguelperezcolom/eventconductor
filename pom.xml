<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.mateu.workflow</groupId>
  <artifactId>eventconductor</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>eventconductor</name>
  <description>Event-driven workflow orchestration engine for Spring Boot applications</description>
  <url>https://github.com/miguelperezcolom/eventconductor</url>

  <licenses>
    <license>
      <name>MIT License</name>
      <url>http://www.opensource.org/licenses/mit-license.php</url>
    </license>
  </licenses>

  <scm>
    <connection>scm:git:git@github.com:miguelperezcolom/eventconductor.git</connection>
    <developerConnection>scm:git:git@github.com:miguelperezcolom/eventconductor.git</developerConnection>
    <url>https://github.com/miguelperezcolom/eventconductor</url>
  </scm>

  <developers>
    <developer>
      <name>Miguel Pérez Colom</name>
      <url>https://github.com/miguelperezcolom/</url>
    </developer>
  </developers>

  <modules>
    <module>modules/workflow-engine</module>
      <module>modules/workflow-e2e</module>
      <module>modules/forms-engine</module>
      <module>modules/rule-runtime</module>
      <module>modules/rule-engine</module>
      <module>modules/workflow-maven-plugin</module>
      <module>modules/sample-worker</module>
      <module>apps/orchestrator-standalone-app</module>
      <module>apps/forms-standalone-app</module>
      <module>apps/rule-standalone-app</module>
      <module>apps/worker-standalone-app</module>
    <module>apps/dev-app</module>
      <module>modules/shared</module>
  </modules>

  <properties>
    <mateu.version>3.0-alpha.243</mateu.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>3.5.7</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-dependencies</artifactId>
            <version>2025.0.0</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-model</artifactId>
            <version>1.0.7</version>
        </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>

      <dependency>
          <groupId>org.springframework</groupId>
          <artifactId>spring-aspects</artifactId>
      </dependency>

  </dependencies>

  <build>
    <plugins>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.14.1</version>
        <configuration>
          <release>${maven.compiler.release}</release>
        </configuration>
      </plugin>

      <!-- Unit tests -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.4</version>
      </plugin>

      <!-- Integration tests -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
            <configuration>
              <includes>
                <include>**/*IT.java</include>
              </includes>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution>
            <id>attach-sources</id>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>3.6.3</version>
        <configuration>
          <source>21</source>
          <doclint>none</doclint>
          <failOnError>false</failOnError>
        </configuration>
        <executions>
          <execution>
            <id>attach-javadocs</id>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.sonatype.central</groupId>
        <artifactId>central-publishing-maven-plugin</artifactId>
        <version>0.8.0</version>
        <extensions>true</extensions>
        <configuration>
          <publishingServerId>central</publishingServerId>
          <autoPublish>true</autoPublish>
        </configuration>
      </plugin>

        <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <version>0.8.12</version>
            <configuration>
                <excludes>
                    <!-- UI layer - requires browser/Vaadin runtime -->
                    <exclude>io/mateu/workflow/infra/in/ui/**</exclude>
                    <!-- JPA persistence - requires database -->
                    <exclude>io/mateu/workflow/infra/out/persistence/**</exclude>
                    <!-- Kafka / async infrastructure -->
                    <exclude>io/mateu/workflow/infra/out/async/Kafka*</exclude>
                    <exclude>io/mateu/workflow/infra/out/async/*Outbox*</exclude>
                    <exclude>io/mateu/workflow/infra/in/async/*KafkaConsumerConfig*</exclude>
                    <!-- MCP tools - requires AI framework at runtime -->
                    <exclude>io/mateu/workflow/infra/in/mcp/**</exclude>
                    <!-- Spring scheduler / startup runners / REST webhooks -->
                    <exclude>io/mateu/workflow/infra/in/scheduler/**</exclude>
                    <exclude>io/mateu/workflow/infra/in/startup/**</exclude>
                    <exclude>io/mateu/workflow/infra/in/rest/**</exclude>
                    <!-- Spring Boot auto-configuration boilerplate -->
                    <exclude>io/mateu/workflow/autoconfigure/**</exclude>
                    <!-- Infrastructure configuration -->
                    <exclude>io/mateu/workflow/infra/config/**</exclude>
                    <!-- Engine starter -->
                    <exclude>io/mateu/workflow/infra/EngineStarter*</exclude>
                    <!-- Git-based import use case - requires network/git repo -->
                    <exclude>io/mateu/workflow/application/usecases/gitimport/**</exclude>
                    <!-- Classpath-based workflow definition loader -->
                    <exclude>io/mateu/workflow/infra/out/classpath/**</exclude>
                    <!-- Shared outbox events (forms-engine) -->
                    <exclude>io/mateu/workflow/infra/out/shared/**</exclude>
                    <!-- Generated gRPC stubs (rule-runtime) -->
                    <exclude>io/mateu/workflow/rules/grpc/**</exclude>
                    <!-- gRPC adapters - require a running server/channel -->
                    <exclude>io/mateu/workflow/infra/in/grpc/**</exclude>
                    <exclude>io/mateu/workflow/infra/out/grpc/**</exclude>
                </excludes>
            </configuration>
            <executions>
            <execution>
                <id>prepare-agent</id>
                <goals>
                    <goal>prepare-agent</goal>
                </goals>
            </execution>

            <execution>
                <id>report</id>
                <phase>test</phase>
                <goals>
                    <goal>report</goal>
                </goals>
            </execution>

            <execution>
                <id>check-coverage</id>
                <goals>
                    <goal>check</goal>
                </goals>
                <configuration>
                    <rules>
                        <rule>
                            <element>BUNDLE</element>
                            <limits>
                                <limit>
                                    <counter>LINE</counter>
                                    <value>COVEREDRATIO</value>
                                    <minimum>0.85</minimum>
                                </limit>
                            </limits>
                        </rule>
                    </rules>
                </configuration>
            </execution>
        </executions>
        </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      Distributed test suite (TESTING.md §5, DIST-01…DIST-05). Excluded from the
      default build because it needs Docker (Testcontainers: PostgreSQL + Kafka)
      and takes minutes. Run with: mvn -Pdist-e2e -pl modules/workflow-dist-e2e -am verify
    -->
    <profile>
      <id>dist-e2e</id>
      <modules>
        <module>modules/workflow-dist-e2e</module>
      </modules>
    </profile>

    <!--
      GPG signing only kicks in when the "release" profile is active, which is
      what the build-and-publish workflow does. CI builds (mvn verify) don't
      have a GPG key and would otherwise fail on the sign goal.
    -->
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>1.6</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals>
                  <goal>sign</goal>
                </goals>
                <configuration>
                  <gpgArguments>
                    <arg>--pinentry-mode</arg>
                    <arg>loopback</arg>
                  </gpgArguments>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
