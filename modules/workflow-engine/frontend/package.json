{
  "name": "eventconductor-workflow-graph",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Standalone Lit web component that renders an EventConductor workflow definition as an ELK graph. Built into a single self-contained ESM served from the engine jar and loaded on demand by mateu's Element/import mechanism.",
  "scripts": {
    "build": "vite build"
  },
  "dependencies": {
    "elkjs": "^0.11.1",
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
