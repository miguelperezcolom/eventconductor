{
  "name": "eventconductor-form-editor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Standalone Lit web component that edits an EventConductor form definition (fields + live preview). Built into a single self-contained ESM served from the forms-engine jar and loaded on demand by mateu's Element/import mechanism.",
  "scripts": {
    "build": "vite build"
  },
  "dependencies": {
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
